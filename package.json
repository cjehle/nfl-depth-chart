{
  "name": "depth-charts",
  "version": "2.0.0",
  "private": true,
  "description": "All-sports depth charts (NFL, NHL, NBA, MLS, college FB & hoops) — starting lineups on the field/ice/court/pitch, live from ESPN.",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test"
  }
}
